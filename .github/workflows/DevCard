# This is a basic workflow to help you get started with Actions

name: DevCard

#First action is if there is a push on the master branch
#Second is a schedule, which acts as a cronjob and will return every {x} time.
#In our case, we will run it every night at 00:00.
on:
  workflow_dispatch:
  push:
    branches:
      - main
  schedule:
    - cron: "0 0 * * *"
#Create a new job that will run the DevCard GitHub action.
#We need to set one variable for our version, 
#which will be the ID of the DevCard we are fetching.
jobs:
  devcard:
    runs-on: ubuntu-latest
    steps:
      - name: devcard
        uses: dailydotdev/action-devcard@2.0.2
        with:
          devcard_id: ${{ secrets.DEVCARD_ID }}
